// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	site: 'https://iwatebison.github.io',
	base: '/engineering-notes',
	trailingSlash: 'always',
});
